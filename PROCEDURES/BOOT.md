# Boot (/boot)

Mid-session recovery (terminal closed, context lost):

1. `git status && git diff --stat`
2. `git log -3 --oneline`
3. Check if build passes
4. Report: last commit, uncommitted changes, build status, recommendation
