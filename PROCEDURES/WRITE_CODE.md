# Writing Code

1. Tag every section with `// @tag-name`
2. Update PROJECT_SPECIFICS.md immediately after adding a tag
3. No orphan tags — every tag in code must be in PROJECT_SPECIFICS, and vice versa
